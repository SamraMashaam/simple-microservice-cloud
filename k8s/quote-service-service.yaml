apiVersion: v1
kind: Service
metadata:
  name: quote-service
  labels:
    app: quote-service
spec:
  selector:
    app: quote-service
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
  type: ClusterIP
